"""Gepard TTS — package root.

Subpackages: `config` (Hydra schema + loaders + validation), `model`
(architecture, losses, self-describing checkpoint config), `data` (collator,
sampling, preprocessing, DPO stages), `training` (SFT + DPO engines),
`inference` (GepardRunner / TTSRunner), `logging`, `cli` (entry points).
"""
